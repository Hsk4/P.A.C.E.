name: scheduler_backend
description: A simple Dart Frog API backend for the personal scheduler app
version: 1.0.0
repository: https://github.com/yourusername/scheduler_backend

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  dart_frog: ^1.1.0
  postgres: ^3.2.1

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.0